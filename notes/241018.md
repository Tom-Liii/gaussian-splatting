# another merge strategy
- initialize a decision mask network at the first place
- use the network through the whole training
- every time need to merge -> randomly select points and feed to the network, let the network to decide which to merge, which to keep